# Twin Kingdoms Inventory
# Beide Burgen, ein Bauplan.

all:
  children:
    kingdoms:
      hosts:
        rheinstein:
          ansible_host: 82.165.165.199
          ansible_user: deploy
          ansible_ssh_private_key_file: ~/.ssh/strato_vps
          deploy_user: deploy
          swap_size: "4G"
          timezone: "Europe/Berlin"

        # nordwall:
        #   ansible_host: 203.0.113.42            # Platzhalter, echte IP vor Deployment eintragen
        #   ansible_user: admin
        #   ansible_ssh_private_key_file: ~/.ssh/nordwall_key
        #   deploy_user: admin
        #   swap_size: "4G"
        #   timezone: "Europe/Berlin"
