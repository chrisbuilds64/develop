services:
  plowcast:
    build: .
    container_name: plowcast
    restart: unless-stopped
    env_file: .env.local
    expose:
      - "8000"
    labels:
      - "caddy=plowcast.chrisbuilds64.com"
      - "caddy.reverse_proxy={{upstreams 8000}}"
