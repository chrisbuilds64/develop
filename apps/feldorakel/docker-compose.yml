services:
  plowcast:
    build: .
    container_name: plowcast
    restart: unless-stopped
    env_file: .env.local
    expose:
      - "8000"
    networks:
      - caddy-net

networks:
  caddy-net:
    external: true
